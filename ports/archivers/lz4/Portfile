# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4
# $Id$

PortSystem          1.0
PortGroup           cmake 1.0

name                lz4
categories          archivers
svn.revision        110
version             r${svn.revision}
revision            0
license             New BSD
maintainers         openmaintainer

description         a very fast lossless compression algorithm
long_description \
   LZ4 is a very fast lossless compression algorithm, providing \
   compression speed at 300 MB/s per core, scalable with multi-cores \
   CPU. It also features an extremely fast decoder, with speeds up and \
   beyond 1GB/s per core, typically reaching RAM speed limits on \
   multi-core systems.

homepage            http://code.google.com/p/lz4/
fetch.type          svn
svn.url             http://lz4.googlecode.com/svn/trunk
worksrcdir          trunk/cmake
use_parallel_build  no

configure.args-append   -DBUILD_SHARED_LIBS:BOOL=ON

livecheck.type      regex
livecheck.url       ${homepage}
livecheck.regex     {>(r[0-9]+)<}
