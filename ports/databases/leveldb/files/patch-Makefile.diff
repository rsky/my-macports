--- Makefile.orig
+++ Makefile
@@ -83,8 +83,8 @@
 SHARED_MAJOR = 1
 SHARED_MINOR = 18
 SHARED1 = libleveldb.$(PLATFORM_SHARED_EXT)
-SHARED2 = $(SHARED1).$(SHARED_MAJOR)
-SHARED3 = $(SHARED1).$(SHARED_MAJOR).$(SHARED_MINOR)
+SHARED2 = libleveldb.$(SHARED_MAJOR).$(PLATFORM_SHARED_EXT)
+SHARED3 = libleveldb.$(SHARED_MAJOR).$(SHARED_MINOR).$(PLATFORM_SHARED_EXT)
 SHARED = $(SHARED1) $(SHARED2) $(SHARED3)
 $(SHARED1): $(SHARED3)
 	ln -fs $(SHARED3) $(SHARED1)
